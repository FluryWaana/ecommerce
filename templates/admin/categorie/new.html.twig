{% extends 'admin/base.html.twig' %}

{% block title %}Création d'une catégorie{% endblock %}

{% block body %}
    <div class="container">
        <div class="card">
            <div class="card-header">
                <div class="row">
                    <h1 class="col"> Création d'une catégorie</h1>
                    <div class="col-2 text-right my-auto">
                        <a href="{{ path('categorie_index') }}" style="font-size: 1.8em;"><i class="fas fa-arrow-circle-left"></i></a>
                    </div>
                </div>
            </div>

            <div class="card-body">
                {{ include('admin/categorie/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
