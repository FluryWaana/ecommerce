{% extends 'admin/base.html.twig' %}

{% block title %}Modification utilisateur{% endblock %}

{% block body %}
    <div class="container">
        <div class="card">
            <div class="card-header">
                <div class="row">
                    <h1 class="col"> Modifier d'un nouvel utilisateur</h1>
                    <div class="col-2 text-right my-auto">
                        <a href="{{ path('user_index') }}" style="font-size: 1.8em;"><i class="fas fa-arrow-circle-left"></i></a>
                    </div>
                </div>
            </div>

            <div class="card-body">
                {{ include('admin/user/_form.html.twig', {'button_label': 'Update'}) }}

                {{ include('admin/user/_delete_form.html.twig') }}
            </div>
        </div>
    </div>


{% endblock %}
